name: Run MapleFly v11 JUMP Learning Experiment
# authoritative H1 CURRENT-only rerun

on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - ".github/workflows/experiment-v11g4-reward-advantage.yml"

permissions:
  contents: read

jobs:
  experiment:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: "24"

      - name: Syntax check
        run: node --check scripts/experiment-v11h1-matched-looming-screen.mjs

      - name: Cache pinned MaleCNS assets
        uses: actions/cache@v6
        with:
          path: .cache/maplefly-connectome
          key: maplefly-male-cns-95a3dbcb05241b0a5c07028ca8ad945b23fbbe6e

      - name: Run v11H1 matched broad looming screen
        run: node scripts/experiment-v11h1-matched-looming-screen.mjs

      - name: Upload experiment result
        id: upload
        uses: actions/upload-artifact@v7
        with:
          name: maplefly-v11h1-matched-looming-${{ github.run_id }}
          path: results/experiment-v11h1
          if-no-files-found: error
          retention-days: 30

      - name: Print artifact evidence
        env:
          ARTIFACT_ID: ${{ steps.upload.outputs.artifact-id }}
          ARTIFACT_DIGEST: ${{ steps.upload.outputs.artifact-digest }}
        run: |
          echo "V11H1-ARTIFACT-ID=${ARTIFACT_ID}"
          echo "V11H1-ARTIFACT-DIGEST=${ARTIFACT_DIGEST}"
          test -f results/experiment-v11h1/experiment_v11h1.json
